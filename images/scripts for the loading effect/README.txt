Island simple cursor effect

Changes:
- Removed the big pop-up loading text.
- Kept the logo as the cursor.
- Kept the 45-degree diagonal cursor angle.
- Loading animation now happens only inside the cursor box, same size as the cursor.
- Removed neon colors; simple black logo only.

Edit in style.css:
--cursor-size: 72px;
--cursor-angle: -45deg;
